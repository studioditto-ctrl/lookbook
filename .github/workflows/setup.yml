name: chat_id 확인

# TELEGRAM_BOT_TOKEN 만 등록해두면 chat_id 를 찾아서 로그에 출력하고,
# 채팅방이 하나면 텔레그램으로 확인 메시지도 보낸다.
# 브라우저로 getUpdates URL 을 직접 열 필요가 없다.
on:
  workflow_dispatch:
    inputs:
      send_test_message:
        description: 확인 메시지 보내기
        type: boolean
        default: true

jobs:
  whoami:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip
      - run: pip install -r requirements.txt
      - name: 시크릿 확인
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        run: |
          missing=0
          if [ -z "$TELEGRAM_BOT_TOKEN" ]; then
            echo "::error title=TELEGRAM_BOT_TOKEN 이 비어 있습니다::Settings → Secrets and variables → Actions 의 'Repository secrets' 에 등록하세요. 'Variables' 탭이나 Codespaces/Dependabot 시크릿은 이 워크플로에서 읽히지 않습니다."
            missing=1
          fi
          exit $missing

      - name: chat_id 조회
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        run: |
          python src/whoami.py \
            ${{ inputs.send_test_message == false && '--no-test-message' || '' }}
